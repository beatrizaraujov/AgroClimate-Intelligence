# Token de autenticação da API GraphQL do MapBiomas Alerta.
# Obtenha o seu em https://plataforma.alerta.mapbiomas.org após criar uma conta.
# Usado apenas no servidor (app/api/mapbiomas/route.ts) — nunca é exposto ao cliente.
MAPBIOMAS_TOKEN=
